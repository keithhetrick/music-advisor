# Where analysis folders live
OUTPUT_ROOT="${OUTPUT_ROOT:-$HOME/music-advisor/features_output}"

# Engine location (use vendor copy OR grant Automator access if pointing to Desktop)
ENGINE_ROOT="${ENGINE_ROOT:-$HOME/music-advisor/vendor/MusicAdvisor}"

# Use the AudioTools venv for both steps
ENGINE_PY="${ENGINE_PY:-$HOME/music-advisor/.venv/bin/python}"
AUDIOTOOLS_PY="${AUDIOTOOLS_PY:-$HOME/music-advisor/.venv/bin/python}"

# Python package name (usually 'MusicAdvisor')
PACKAGE_NAME="MusicAdvisor"

# Enable extractor for Quick Action (stub now; replace internals later)
AUTOMATOR_CLI="${AUTOMATOR_CLI:-$HOME/music-advisor/scripts/extract_from_audio.sh}"

# macOS notifications
NOTIFY=1
