# Environment overrides for automator.sh / automator_full.sh
# Copy to config/automator.env and edit as needed. All values are optional.

# Python executable to use (defaults to repo-local .venv/bin/python)
# PY_BIN="/Users/you/music-advisor/.venv/bin/python"

# Override the sidecar command (defaults to python tools/tempo_sidecar_runner.py --audio {audio} --out {out})
# SIDECAR_CMD="/Users/you/music-advisor/.venv/bin/python tools/tempo_sidecar_runner.py --audio {audio} --out {out} --backend auto"

# Tempo confidence bounds for filtering Essentia/Madmom sidecars
# TEMPO_CONF_LOWER=0.9
# TEMPO_CONF_UPPER=3.6

# QA policy and clipping behavior
# QA_POLICY=default        # or strict / lenient
# FAIL_ON_CLIPPING_DBFS=-0.5   # set to empty to disable hard fail (warn only)

# Logging / redaction
# LOG_REDACT=1
# LOG_REDACT_VALUES="/Users/you/Music"

# Cache directory override
# CACHE_DIR="/Users/you/music-advisor/features_output"

# Optional: pass through extra args to ma_audio_features.py (quoted)
# EXTRA_FEATURE_ARGS="--tempo-sidecar-verbose"
