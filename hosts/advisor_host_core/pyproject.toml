[project]
name = "musicadvisor-host-core"
version = "0.0.1"
description = "Host contracts/helpers relocated from ma_host."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
