# Commands (BuilderPack v0.3.5)

- `/audio import` → expects payload matching v0.3.5 schema
- `/advisor run full` → advisory only; HCI_v1 from external engine
- `/lyrics analyze` → advisory only (no effect on HCI)
