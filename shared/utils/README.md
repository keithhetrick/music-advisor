# shared/utils

Utilities namespace to house shared helpers (currently re-exporting `shared.core`).

- Entry point: `shared/utils/__init__.py` (imports from `shared.core` today).
- Use this package for future common helpers instead of scattering under engines/hosts.
